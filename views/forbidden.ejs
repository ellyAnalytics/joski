<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Access Denied</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <main class="container mx-auto flex-1 p-6">
    <% if (session && session.error) { %>
      <div id="alert" class="max-w-xl mx-auto mt-4 p-4 bg-red-100 text-red-700 border border-red-300 rounded shadow flex justify-between items-center transition-opacity duration-700">
        <span><%= session.error %></span>
        <button onclick="document.getElementById('alert').remove()" class="ml-4 text-red-700 hover:text-red-900 font-bold">&times;</button>
      </div>
    <% } %>
    <h1 class="text-2xl font-bold mt-6 text-center">🚫 Access Denied</h1>
    <p class="text-center mt-2">You don’t have permission to view this page.</p>
    <div class="text-center mt-4">
      <a href="/dashboard" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">Go Back</a>
    </div>
  </main>

  <script>
    setTimeout(() => {
      const alertEl = document.getElementById('alert');
      if(alertEl){
        alertEl.style.opacity = '0';
        setTimeout(() => alertEl.remove(), 700);
      }
    }, 6500);
  </script>

</body>
</html>
